<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="false"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true"
            android:screenOrientation="portrait">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Custom scheme deep link -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="rematcher-exchange" android:host="intake" />
                <data android:scheme="rematcher-exchange" android:host="app" />
            </intent-filter>

            <!-- HTTPS App Links — Field Test (autoVerify when assetlinks.json + signing fingerprint match) -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="field-test-exchange.rematcher.co.il" android:pathPrefix="/intake" />
                <data android:scheme="https" android:host="field-test-exchange.rematcher.co.il" android:pathPrefix="/home" />
                <data android:scheme="https" android:host="field-test-exchange.rematcher.co.il" android:pathPrefix="/demand" />
                <data android:scheme="https" android:host="field-test-exchange.rematcher.co.il" android:pathPrefix="/inventory" />
                <data android:scheme="https" android:host="field-test-exchange.rematcher.co.il" android:pathPrefix="/matches" />
                <data android:scheme="https" android:host="field-test-exchange.rematcher.co.il" android:pathPrefix="/opportunities" />
                <data android:scheme="https" android:host="field-test-exchange.rematcher.co.il" android:pathPrefix="/activity" />
            </intent-filter>

            <!-- HTTPS App Links — Production -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="exchange.rematcher.co.il" android:pathPrefix="/intake" />
                <data android:scheme="https" android:host="exchange.rematcher.co.il" android:pathPrefix="/home" />
                <data android:scheme="https" android:host="exchange.rematcher.co.il" android:pathPrefix="/demand" />
                <data android:scheme="https" android:host="exchange.rematcher.co.il" android:pathPrefix="/inventory" />
                <data android:scheme="https" android:host="exchange.rematcher.co.il" android:pathPrefix="/matches" />
                <data android:scheme="https" android:host="exchange.rematcher.co.il" android:pathPrefix="/opportunities" />
                <data android:scheme="https" android:host="exchange.rematcher.co.il" android:pathPrefix="/activity" />
            </intent-filter>
        </activity>

        <!-- WhatsApp / Gallery Share Sheet — one share action = one IntakeBatch -->
        <activity
            android:name=".share.ShareReceiverActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
                <data android:mimeType="text/plain" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Camera via web file capture / future Capacitor Camera -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- POST_NOTIFICATIONS for Android 13+ when native FCM is wired (Owner Firebase) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
</manifest>
