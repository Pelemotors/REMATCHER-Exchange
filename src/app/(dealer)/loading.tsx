import { ActionCardLoadingSkeleton } from "@/components/ui/brand-v2";

/** Default dealer segment — action/card pattern (Home/Matches-like). */
export default function DealerLoading() {
  return <ActionCardLoadingSkeleton />;
}
