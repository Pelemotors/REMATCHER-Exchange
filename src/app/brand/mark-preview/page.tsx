import { ExchangeMarkPreview } from "@/components/brand/exchange-mark-preview";

export const metadata = {
  title: "Exchange Mark Preview",
  robots: { index: false, follow: false },
};

export default function ExchangeMarkPreviewPage() {
  return <ExchangeMarkPreview />;
}
